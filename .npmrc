strict-peer-dependencies=false
# Expo's native build scripts resolve modules from a flat node_modules; pnpm's
# nested store gave nine copies of expo-constants and broke the app.config
# script phase during an iOS build. Hoisting is what Expo recommends for pnpm.
node-linker=hoisted
