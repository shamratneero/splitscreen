# Required for production: use the identifier registered to your Apple account.
# IOS_BUNDLE_IDENTIFIER=com.yourcompany.addasplit
# The preview QR opens a sample bill. Publishing real drafts still needs backend integration.
EXPO_PUBLIC_GUEST_URL=http://localhost:3000
