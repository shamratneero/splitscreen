# Public — safe in the browser bundle; guests reach data only through
# security-definer RPCs and RLS.
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon key>
