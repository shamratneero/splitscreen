{
  "name": "splitsave",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.18.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "corepack pnpm --filter @splitsave/split-engine test && corepack pnpm --filter @splitsave/receipt-parser test && corepack pnpm --filter @splitsave/types test",
    "typecheck": "corepack pnpm --filter @splitsave/split-engine typecheck && corepack pnpm --filter @splitsave/guest-web typecheck && corepack pnpm --filter @splitsave/host-mobile typecheck && corepack pnpm --filter @splitsave/receipt-parser typecheck && corepack pnpm --filter @splitsave/types typecheck",
    "dev:guest": "corepack pnpm --filter @splitsave/guest-web dev",
    "dev:host": "corepack pnpm --filter @splitsave/host-mobile start",
    "dev:host:web": "corepack pnpm --filter @splitsave/host-mobile web",
    "test:host:web": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  },
  "engines": {
    "node": "22.x"
  }
}
