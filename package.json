{
  "name": "splitpay",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.18.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "corepack pnpm --filter @splitpay/split-engine test && corepack pnpm --filter @splitpay/receipt-parser test",
    "typecheck": "corepack pnpm --filter @splitpay/split-engine typecheck && corepack pnpm --filter @splitpay/guest-web typecheck && corepack pnpm --filter @splitpay/host-mobile typecheck && corepack pnpm --filter @splitpay/receipt-parser typecheck",
    "dev:guest": "corepack pnpm --filter @splitpay/guest-web dev",
    "dev:host": "corepack pnpm --filter @splitpay/host-mobile start",
    "dev:host:web": "corepack pnpm --filter @splitpay/host-mobile web",
    "test:host:web": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  },
  "engines": {
    "node": "22.x"
  }
}
